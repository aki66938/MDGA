# MDGA 开发历程

项目代号：MDGA — Make DeepSeek Great Again
仓库地址：https://github.com/aki66938/MDGA
主创：aki66938

版本号规则：`主版本.里程碑版本.功能版本`

---

| 版本 | 更新描述 | 开发者 |
|------|----------|--------|
| 0.0.1 | 项目初始化，建立 Tauri 2 + Rust workspace + React 桌面应用骨架<br>实现 DeepSeek API 流式聊天（SSE），支持 Enter 发送、Shift+Enter 换行<br>实现 API Key 环境变量检测（DEEPSEEK_API_KEY），状态栏显示配置状态<br>实现 Token 用量统计：展示总 token、输入/输出、缓存命中、估算费用<br>实现 assistant 回复 Markdown 渲染（react-markdown + remark-gfm）<br>assistant 消息无气泡背景融入页面，token 统计行独立显示于内容下方<br>建立 GitHub Actions CI/CD，tag 推送自动构建 Windows 安装包并发布到 Release | Claude Code / Codex |
| 0.0.2 | 实现会话持久化：SQLite 存储，应用重启后历史对话保留<br>实现多会话管理：侧边栏显示历史对话列表，支持新建、切换、删除会话<br>首条消息自动命名会话标题（取前 20 字），无需手动操作<br>流式回复结束后自动保存 assistant 消息及 token 统计到本地数据库 | Claude Code |
| 0.0.3 | 更新 DeepSeek 模型选择：默认使用 `deepseek-v4-flash`，并提供 `deepseek-v4-pro` 可选<br>移除界面中的旧模型别名 `deepseek-chat` / `deepseek-reasoner`，避免继续使用即将废弃的模型 ID<br>按所选模型匹配 token 费用估算价格，提升账本对照准确性 | Codex |
| 0.0.4 | 增加当前会话累计 token 与估算费用展示，历史会话加载后可基于已保存 usage 自动聚合<br>统一单次回复与会话累计费用格式，便于用户观察消耗水平并与账单对照<br>补充前端测试覆盖持久化 usage 聚合展示 | Codex |
| 0.0.5 | 增加当前工作区绑定：用户可输入本地目录路径并保存为 Agent 授权工作区<br>工作区信息写入 SQLite，应用重启后可恢复显示<br>后端保存前校验路径存在且为目录，为后续权限与文件任务边界打基础 | Codex |
| 0.0.6 | 重构工作区绑定为新对话 session 级选择：首屏通过系统目录选择器选择工作区，普通对话正文不再显示路径输入表单<br>conversation 表新增 workspace snapshot 与 mode 字段，发送首条消息时将所选目录绑定到整轮会话<br>接入 Tauri dialog 插件与权限配置，新增前端测试、storage 测试并通过全量 Rust / 前端验证 | Codex |
| 0.0.7 | 打通 workspace 认知闭环：发送消息时前端传入 conversationId，后端从 SQLite 读取该会话 workspace snapshot<br>调用 DeepSeek 前自动注入 system workspace context，使模型能够回答当前会话绑定的工作区路径<br>补充单会话 workspace 查询、system message 注入与前端发送参数测试 | Codex |
| 0.0.8 | 打通首个真实本地工具闭环：DeepSeek 可通过 Tool Calls 请求 `create_file`，MDGA 后端在会话工作区内真实创建文件<br>新增 workspace path guard，拒绝绝对路径和 `..` 越界写入，目标文件存在时拒绝覆盖<br>补充 DeepSeek tool-call 解析、Rust tool-runtime 与桌面后端桥接测试 | Codex |
| 0.0.9 | 实现完整 Agent Tool Runtime：模型可自主调用工具并多轮推理，最多 5 轮工具循环<br>新增文件移动（move_path）、目录删除（delete_dir）、本地命令执行（run_command）工具<br>修复 DSML 双竖线解析 Bug，确保模型内嵌工具调用格式能被正确识别和执行<br>新增权限模式选择器（Restricted / AskEveryTime / WorkspaceAuto / FullAccess），run_command 仅 FullAccess 可用<br>工具执行记录持久化到 SQLite，前端工具事件面板实时展示每步执行结果 | Claude Code |
| 0.0.10 | Agent Loop 控制力强化：工具循环轮数上限从 5 提升到 20，复杂多步任务不再被过早截断<br>多轮过程流式可见：模型调用工具前的叙述内容实时推送，不再黑盒等待<br>新增中断能力：发送中显示「停止」按钮，用户可随时中止 Agent 任务，已执行结果保留 | Claude Code |
| 0.0.11 | 工具执行过程内联渲染：AI 的工具调用直接插入对话流，与叙述文字交错显示，告别底部扎堆面板<br>工具卡片实时展示执行状态：运行中显示动画、完成显示绿色 ✓、失败显示红色 ✗ 与错误原因<br>用户视角下 AI「思考到哪一步、调用了什么工具」一目了然，体验连贯不黑盒 | Claude Code |
| 0.0.12 | 权限审批闭环：修复「每次询问」模式下连读文件都报错的问题，只读操作直接放行，仅写/删/命令才弹审批<br>新增高风险动作审批弹窗：Agent 执行越界或敏感操作前弹框请用户「允许一次/拒绝」<br>低风险命令白名单：cargo check/test、git status、npm test 等常用命令在工作区自动模式下免审批直接执行<br>修复 DSML 工具标记泄漏：任务步数过多撞上限时，不再把内部工具标记当正文显示，改为清晰的上限提示 | Claude Code |
| 0.0.13 | 修复工具执行记录重启后丢失：工具卡片与叙述文字的交错结构现已持久化，重启或切换会话后完整还原<br>历史会话加载时还原内联工具调用记录（✓/✗/⊘ 状态、目标路径），不再只剩纯文字<br>旧版本数据兼容加载，自动升级数据库结构 | Claude Code |
| 0.0.14 | 项目结构认知：会话开局自动注入工作区目录树摘要，模型无需逐层探查即可了解项目骨架<br>工具失败自纠引导：工具执行失败时引导模型读取错误、调整方案后重试，而非反复重复同一次失败调用<br>网络抖动自动重试：DeepSeek API 偶发网络错误 / 服务端错误 / 限流时自动退避重试（最多 4 次），长任务不再因一次网络波动中断 | Claude Code |
| 0.0.15 | 上下文自动压缩（auto-compact）：长任务上下文接近上限时先压缩较早的工具输出，仍超限则把早期对话自动总结为任务进度摘要，对话持续进行不再卡住<br>Agent 工作状态实时可见：思考中（第 N 轮）、执行工具、压缩上下文、输出回复均实时显示并附耗时计秒，不再黑盒等待<br>状态栏新增上下文用量百分比；压缩发生时对话流内显示通知卡片<br>支持工作区根目录 MDGA.md 项目长期记忆文件，跨会话持久注入项目目标与约定，不被压缩冲掉<br>压缩阈值支持环境变量 MDGA_CONTEXT_SOFT_LIMIT 调节，便于验证压缩机制 | Claude Code |
| 0.0.16 | 取消工具调用轮数上限：Agent 任务不再被 20 步硬上限截断，复杂任务可持续推进至自然完成（上下文压缩兜底体积，停止按钮兜底失控）<br>连续工具调用折叠显示：执行中实时展示当前动作，完成后自动折叠为一行摘要，点击可展开查看每一步<br>新增 Plan13 对标路线文档：以 Claude Code / Codex 为最终形态目标的功能补齐总规划 | Claude Code |
| 0.0.17 | 变更管理：文件修改在对话流内显示彩色 diff（+N/−M 行），每轮变更自动快照，「变更」面板可一键回退任意改动<br>任务系统：Agent 自维护任务清单实时显示进度；新增计划模式（先出计划确认后执行）与只读子代理（run_subtask）<br>命令增强：命令输出实时流式显示、支持后台运行；审批新增「总是允许」记忆，同类动作免重复审批<br>输入增强：斜杠命令（/compact、/clear、/init、/rewind、/model）与 @ 文件引用补全；📎 文档导入（TXT/MD/CSV/PDF/DOCX）总结问答<br>MCP 接入：设置页可添加/启停 MCP 服务器，外部工具统一进权限审批与审计；Skills 技能体系（.mdga/skills 渐进披露加载）<br>体验补齐：代码高亮+复制、会话搜索/重命名/置顶/归档、设置页（移至侧边栏底部）、错误提示人话化<br>修复：API 请求无超时导致的「永久思考」隐患；&lt;ToolCall&gt; 工具标记泄漏新变体；纯聊天会话幻觉工具调用（明确提示需绑定工作区，顶栏显示「纯聊天」标识） | Claude Code |
| 0.0.18 | 全新 UI 设计系统（贴近 DeepSeek 品牌）：DeepSeek 蓝主色、亮色 / 深色「深海」双主题、Lucide 线性图标全面替换 emoji、MDGA 自有深海标识<br>布局精修对标 CC/Codex：顶栏精简为工作区 / 上下文 / 变更胶囊；权限模式、模型、计划模式下移至输入框上方控制行（随时可切，下一轮生效）；composer 改为左附件 + 输入 + 圆形发送三段式<br>思考指示由旋转圆圈改为柔和脉冲三点，更贴近「思考中」观感<br>上下文压缩软上限由 96K 提升至 800K（贴合 V4 1M 标称，留 headroom）<br>设置页新增账户余额：调用 DeepSeek 官方接口显示余额状态与各币种总额 / 充值 / 赠送明细，可手动刷新<br>新增 Plan14 UI 设计系统文档 | Claude Code |
| 0.0.19 | 联网能力：新增 web_search（联网搜索）与 web_fetch（抓取网页正文）工具，Agent 可查文档 / 查报错 / 查最新资料<br>流式叙述：Agent 工作时叙述逐字流式输出，不再整块跳出；内置防泄漏守卫杜绝内部工具标记外显<br>并行只读工具：一轮内多个只读操作（读多文件 / 抓多 URL）并发执行，提速明显<br>自动记忆：新增 remember 工具，Agent 可自主把项目约定 / 关键路径沉淀到 MDGA.md，跨会话生效<br>沙箱加固：命令子进程擦除敏感环境变量（API Key / Token / 密码），防止 Agent 命令读取或外泄凭据<br>工具运行动画由快速旋转圆圈改为四角星柔和闪动 | Claude Code |
| 0.0.20 | Agent 基础设施四件地基：① 大文件分页读取（read_file 支持 offset/limit，模型可分段翻页）② 细粒度权限规则（deny 优先 + 按工具/路径 glob，如 deny:read_file:**/.env）③ 运行中插话 Steering（Agent 工作时输入排队，下一轮注入纠偏，不打断任务）④ Hooks 生命周期（.mdga/hooks.json 的 PreToolUse 可拦截工具、PostToolUse 后处理）<br>设置页重做为左栏分类导航 + 右栏内容双栏，加入各项说明；弹窗加宽<br>思考与工具运行图标统一为四角星缓转闪动（DeepSeek 蓝） | Claude Code |
| 0.0.21 | 修复自动更新：开启 updater 清单产物（createUpdaterArtifacts），CI 发版起会生成并发布 latest.json，客户端「检查更新」从此可真正发现新版并自动下载安装<br>检查更新全程反馈：检查中 / 已是最新 / 发现新版本 / 失败原因，不再点了无反应 | Claude Code |
| 0.0.22 | 搜索升级为 ripgrep 同源引擎：自动尊重 .gitignore、跳过隐藏/噪声文件，支持正则，搜大型项目又快又准<br>上下文缓存优化：工作区结构摘要按会话缓存，提升 DeepSeek 缓存命中、降低费用与延迟<br>模型容灾：主模型持续不可用（过载/限流）时自动切换同族备用模型继续<br>托管后台命令：后台命令返回句柄，Agent 可轮询输出、查看状态、随时终止<br>检查更新按钮交互优化：结果直接在按钮上显示 10 秒、期间禁用防误触，尺寸不变 | Claude Code |
| 0.0.23 | 命令沙箱（受限令牌，M8.1）：run_command 在降权的受限令牌 + Job Object 沙箱中执行——剥离管理员特权、进程随会话干净销毁、子进程环境擦除密钥；失败即拒绝执行不降级<br>设置「模型与权限」新增命令沙箱开关，少数需特权命令受阻时可临时关闭<br>（网络与文件路径隔离将由后续 AppContainer 版本 M8.2 提供） | Claude Code |
| 0.0.24 | 成本预算：可设单任务 token 上限，超额自动暂停防止失控烧 token（费用透明卖点强化）<br>数据治理：设置新增「数据」分类，支持导出会话（Markdown）、导出 Token 账本（CSV，可与官方账单对账）、清除所有会话<br>自定义斜杠命令：工作区 .mdga/commands/*.md 自动进斜杠菜单，$ARGUMENTS 传参<br>自定义子代理类型：run_subtask 可指定 agentType 加载 .mdga/agents 角色定义<br>诊断反馈环：配置 .mdga/diagnostics 后，Agent 改完代码收尾前自动跑检查，有错自动修复 | Claude Code |
| 0.0.25 | MCP HTTP 传输：MCP 服务器除 stdio 外支持 http(s):// 地址（Streamable HTTP，自动识别 JSON / SSE 响应），可填静态 Bearer Token 接入大量托管服务<br>MCP OAuth 2.1（PKCE 授权码流，浏览器授权 + token 自动持久化）⚠️ **未经真机验证**：缺少需要 OAuth 的真实 MCP 服务器测试环境，代码按规范实现、PKCE 经 RFC 7636 官方测试向量验证，但端到端流程待后续真机确认<br>修复 Agent 身份混淆：明确告知模型 MDGA 不是 Claude Code、配置目录是 .mdga/（非 .claude/），杜绝模型沿用训练记忆里的 Claude 约定去找/改 .claude/settings.json<br>新增 add_mcp_server 工具：Agent 可用 MDGA 真实机制（写入服务器表 + 即时连接）为自己注册 MCP，而非编辑无效配置文件 | Claude Code |
| 0.0.26 | 结构化提问：需求含糊时 Agent 弹出选项卡片请你确认（支持单选 / 多选 / 自定义「其他」），不再擅自假设方向<br>异步子代理：探索类子任务可后台运行，立即返回任务号、回头取结果，长调查不阻塞主对话，可随时查看进度或终止<br>代码检索增强：内容搜索新增「仅列文件 / 计数 / 上下文行 / 大小写不敏感 / 跨行 / 按文件类型或文件名过滤」等模式；新增按文件名快速查找文件<br>接入 MCP 资源：可列举并读取已连接 MCP 服务器暴露的资源<br>超大工具输出自动落盘为工作区文件、仅回传摘要与路径，避免撑爆上下文（需要时用 read_file 分页查看） | Claude Code |
| 0.0.27 | 模型供应商（**重要变更**）：API Key 不再从系统环境变量读取，改为在应用内「设置 → 模型供应商」配置并存于本地——升级后需在设置页重新填写 API Key 与模型才能对话<br>多供应商：内置 DeepSeek / 智谱 GLM / 月之暗面 Kimi / 通义 等 OpenAI 兼容预设，Base URL 留空即用官方端点、可展开「高级设置」填自定义（自托管 / 代理）<br>设置页拆分：原「模型与权限」拆为独立的「模型供应商」与「权限」两个分类<br>扩展 agent 的模态：可选接入独立于主模型的视觉（识图）模型；未配置视觉模型时维持现状（纯对话 + 文本文件，不能提交图片 / 图像型 PDF），配置视觉模型后放开图像提交入口（音频为占位，敬请后续） | Claude Code |
| 0.0.28 | 识图真正生效：配好视觉模型后，上传图片 + 文本需求即可让主模型据视觉分析完成任务（设计图→代码、报错截图→修复建议、表格图→数据整理等）；视觉调用以「用户需求」为方向盘做意图驱动的要点化识别，而非泛泛描述<br>视觉供应商同时兼容 OpenAI 与 Anthropic 两种 API 格式，base_url 用户自填，照各平台文档粘贴「基址」或「完整端点」均可——修复了粘贴完整端点时被重复拼接路径（如 `/v1/chat/completions/chat/completions`）导致识图失败的问题（主模型自定义端点同此修复）<br>修复安装版运行时弹出空白命令行黑框：发行版改为纯 GUI 窗口启动，不再附带 cmd 控制台 | Claude Code |
| 0.0.46 | 侧边栏对话记录交互打磨 + 重命名入口(稿 A · 无界渐隐,贴 CC desktop)。**① 标题渐隐替代省略号**:对话标题过长时右缘改为 `mask-image` 渐隐羽化,替代原本的省略号「…」。**② 动作区改覆盖层**:置顶 / 归档 / 删除 + 新增的「重命名」四个图标,从原本「占位内联」(始终挤占标题宽度)改为「绝对覆盖层」——不 hover 时标题占满整行、仅右缘渐隐;hover 整行时四个图标连同「透明 → 侧栏 hover 底色(`--surface-3`)」的渐变一起从右淡入,渐变把图标左侧的标题文字羽化虚化(覆盖层默认 `pointer-events:none` 穿透,点右半空白区照常选中会话)。**③ 新增重命名入口**:动作区首位加「重命名」铅笔图标,复用既有 `rename_conversation` 后端 + 内联改名(此前仅能双击标题触发,入口隐蔽);内联编辑态时隐藏整组图标,避免覆盖层盖住输入框。删除图标 hover 染危险红(`--danger`)。浅 / 深色均走主题变量自适应,前端 typecheck 通过 | Claude Code |
| 0.0.45 | 修复 0.40-0.42 暴露的两个功能 bug + 灯泡图标归位。**① run_command 全 `exit -1073741502` 修复**:0.0.42 文件隔离-A 把工作区(Tauri 项目命中 dev-watch)降级到受限令牌沙箱后,echo/dir/whoami 全部 exit -1073741502(`0xC0000142` STATUS_DLL_INIT_FAILED)——根因是受限令牌进程拿不到交互桌面:CreateProcessAsUserW 的 `lpDesktop` 为 NULL + deny-only Administrators 组改了有效 SID,致降权进程在 DLL 初始化期打开 `winsta0\default` 被拒。这段代码自 M8.1 就在,但受限令牌路径以前 AppContainer 直接跑、从没真跑过命令,0.0.42 降级才首次触发暴露。修复:去掉 deny-only Admin(只 `DISABLE_MAX_PRIVILEGE` 剥特权,已足以挡管理员能力)+ 显式指定 `lpDesktop="winsta0\default"`;另加固——exit_code 落在 NTSTATUS 错误段且无输出时识别为沙箱启动失败、返回 Err 触发 fail-closed,不再静默上报 -1073741502 误导诊断。**② web_search 空结果修复**:根因不是页面结构变化,而是 DuckDuckGo HTML 端点反爬限流(连发几次返回 202 + 需 JS 的挑战页);叠加固定可疑 UA + 代码不看 HTTP 状态码。修复:换真实浏览器 UA + 检查状态码 + 探测挑战页 + 退避重试 3 次,区分「被限流」与「真无结果」。**③ 思考灯泡归位**:「金点子」灯泡移到「思考过程」框(替换 Brain),agent 工作状态行(正在思考 / 执行)恢复四角星——修正上版误把灯泡放到状态行、连执行图标也跟着变了的问题。**④ 思考灯泡「亮起」动画**:此前移到思考过程框的「金点子」灯泡只是静态色块(类注释直写「静态标识不脉冲」),用户期待的「亮起=暖光晕呼吸 + 放射线微张」从没实现——本版补上:`drop-shadow` 暖光晕 0→4px 呼吸 + 整体 `scale(0.96↔1.06)` 使放射线随之微张(2.6s 缓动循环、轻量不喧宾夺主),光晕色用 CSS 变量随浅 / 深色切换(`#F5A623` / `#FBBF24`),并加 `prefers-reduced-motion` 关闭。(状态行四角星本就有缩放 + 透明度脉冲、正常;若仍看不到动画多半是系统「减弱动态效果」开着。)**⑤ 每轮 token 统计行归位**:每组对话(1 问 1 答)的 per-turn token 行(如「36,250 tokens · 35,204 in / 1,046 out · 27,776 cached · $0.004242」)从「独立成行」移到 assistant 回复下方、与复制按钮同一行(并入 MessageActions 操作条:token 行常驻可见,复制 / 重发 / 重生成图标仍 hover 整行或聚焦才淡入),小字号不变;会话累计「总统计」(ConversationUsageSummary)位置与内容完全不动。前端 typecheck 通过 | Claude Code |
| 0.0.44 | 思考相关 UI 细节打磨(承接 0.0.43)。**① 思考过程框**:从「实线 + 实底硬框」改为虚线框 + 透明折叠态 + 展开内容淡品牌蓝底(呼应思考主题、更轻量);caret 旋转过渡并在展开时染品牌蓝、内容淡入展开;浅 / 深双色均用主题变量自适应。**② 思考图标**:agent 思考 / 工作状态前的图标从四角星 ✦(四重对称、整圈旋转几乎看不出)换成「金点子」线性灯泡——暖灯泡色(浅色 `#F5A623` / 深色 `#FBBF24`)+ 环绕放射线,亮起 = 暖光晕呼吸 + 放射线微张,呼应动漫 / 游戏里「想到好点子」的灯泡。前端 typecheck 通过 | Claude Code |
| 0.0.43 | MDGA 应用图标 + 修复终止按钮(对话总开关)。**① 终止按钮(前期逻辑漏洞)**:此前点「停止」无效——cancel flag 置位正常,但 agent 卡在接收模型流式响应的 await 里、检查点只在两次请求的间隙,要等整轮响应收完才生效(短响应像没反应、长响应干等很久)。本版给流式请求套 cancel:用 `tokio::select!` 让「模型流式请求」与「cancel 轮询」赛跑,点停止 ~80ms 内 drop 掉正在进行的请求、当轮立即停;主路径(工具循环)+ 计划模式 + 纯聊天三路全覆盖。**总开关级联**:停止时同步子任务(run_subtask)共享主 cancel 自动停、后台子任务(background)按会话 id 级联置位停,不留孤儿。**输出保留**:取消时 `return Ok(已完成 usage)` → 前端 chat-done 正常落库已流式显示的叙述 + 工具卡片(0.0.42 的 wire 边落库也在),不丢。**停止按钮风格**:图标/配色/动效对齐整体——品牌蓝圆 + 圆角停止方块 + 脉冲光晕(呼应思考/工具四角星动感),替换原本突兀的告警红实心方块。**② 应用图标**:把 MDGA.ico 制成 app 图标(提取 256×256 → tauri-cli 生成多尺寸 `icon.ico`/PNG),exe 与安装包显示 MDGA 图标 | Claude Code |
| 0.0.42 | 任务续接地基 + 沙箱体验/健壮性(承接 0.0.41)。**① 任务续接(数据安全硬伤)**:此前 agent 一轮工具循环的完整 wire 历史只在内存、整轮结束才由前端落库,进程崩溃/API 断额即全丢、"继续"只能凭残留叙述文本重新规划(对标 CC 的"逐事件落盘 + 工具结果配对回放"一根支柱都没有)。本版后端**边执行边落库** wire(每轮请求前 + assistant 声明工具后 + 每个工具结果后,UPSERT 进新 wire_snapshots 表),崩溃/断额后已执行的工具结果不再丢;读回时给"已发出未完成"的工具调用补占位 tool_result(满足 Anthropic「tool_use 必跟 tool_result」、防续接 400)。纯后端、前端零改,为后续"真 resume(自动接着干)"铺好地基(留 0.0.43)。**② 文件隔离防护(修阻断)**:沙箱靠给工作区每个文件加 ACL 实现路径隔离——首次对大项目(含 target/node_modules)授权会卡几十秒、对正被 tauri dev/vite 监视的源码树授权会触发反复 rebuild(在自己这种 Tauri 项目里 dogfood 高发)。本版走 AppContainer 前检测工作区(dev-watch 启发式 + 文件数预探,按工作区缓存),命中即降级受限令牌沙箱(不碰用户文件 ACL),默认倒向降级(误报顶多丢路径隔离、不丢安全)。**③ 沙箱输出实时化(体验)**:命令输出从"跑完才一次性按行回显"改为 reader 边读边逐行回调(按 \n 切行 + 逐行 strip_vt),长命令(cargo/npm build)实时可见,对齐无沙箱路径。**④ 文件隔离结论 + ConPTY 验真计划**:确认不动用户文件 ACL 就没有现成轻量机制做真逐路径隔离(受限令牌只降权;minifilter/job silo/projfs 都需驱动签名或管理员),已记录;ConPTY 跨版本"静态导入是否在 <1809 启动崩"待 Win10 1607 VM 验真(安全底线已由运行时自检 + fail-closed 守住,最坏降级不越权)。真机全量回归通过(tool-runtime 37 + desktop 2 + storage 10) | Claude Code |
| 0.0.41 | 命令沙箱去系统注册表依赖 + 健壮性/隔离强化大批(承接 0.0.40)。**① 去注册表依赖**:实测发现 0.0.40 为让容器内 native 输出回传而设的 `HKCU\Console\LowBoxConsoleEnabled` 注册表键、及配套 `registryRead` 能力,**其实都非必需**——单靠 `lpacCom` 一个能力就够(此前以为必需,是因一直三个一起设、从没单独验证 lpacCom);本版彻底移除 app 对系统注册表的任何写入(不再碰 `HKCU\Console`,杜绝"后台改系统级控制台开关"的侵入与卸载残留)、并去掉 `registryRead` 能力(隔离更严),配方收敛为「ConPTY + lpacCom + New-PSDrive」三件。**② 攻克命令收尾偶发卡死**:深挖发现根因不在进程等待/输出读取(均正常),而在 ACL 授权——给工作区父链授遍历权时,对 `%TEMP%` 这类海量子项目录用高层 API 会触发对全部子项的继承传播、单次卡 ~54 秒;改用底层 API 只设目录自身权限、不递归子项,54s→~1ms。**③ 进程树治理**:命令套 Job Object(KILL_ON_JOB_CLOSE + 进程数/内存配额),超时/取消时整棵进程树(含 native 孙进程)干净销毁、杜绝孤儿与 fork 炸弹。**④ 隔离与并发强化**:容器身份按工作区唯一(每工作区独立沙箱身份/授权,不跨工作区累积权限);**ACL 授权幂等跳过**(工作区已含本容器 ACE 就整段跳过授权、不再每命令重写整棵子树安全描述符——杜绝改子文件时间戳误触发 tauri dev/vite 等文件监视器 rebuild 致应用反复重启,大 repo 也不再每命令卡顿);profile/授权加进程级串行锁防并发竞争;临时目录按命令唯一并收尾删除;SID/句柄全程 RAII 守卫,任一失败路径都不泄漏。**⑤ 逃逸防护**:拒绝向 junction/symlink(重解析点)授读写权,防经其 target 逃出工作区。**⑥ 可观测**:命令结果暴露实际生效的沙箱层级与是否降级。**⑦ 网络逐域名 spike**:论证三约束(不改系统/不需管理员/不写驱动)下逐域名"真边界"做不到(能看域名的代理可被绕过、能强制的 WFP 看不到域名),保持网络全开/全关二选一(本身即 WFP 强制的真边界)、逐域名列未来项。真机全量回归 35 项通过 | Claude Code |
| 0.0.40 | 命令沙箱升级为 AppContainer 真隔离(承接 0.0.39 基座、攻克 native 输出拦路问题)。0.0.39 遗留的拦路问题——容器内 native console 程序(git / npm / node / cmd)的输出不回传——本版攻克并接为默认沙箱:① 补齐之前漏做的微软正规解:`HKCU\Console\LowBoxConsoleEnabled` 开关 + `lpacCom`/`registryRead` 能力,配合 **ConPTY 伪控制台**,容器内 native 命令输出完整回传(并剥离 VT 转义还原为纯文本);② 用 `New-PSDrive` 把工作区挂成 PSDrive 再进入,规避 AppContainer 对用户目录路径的 `Set-Location` 限制,使命令的工作目录与相对路径都正确落在工作区;③ 沙箱开启时**默认走 AppContainer**——真文件路径隔离(容器默认拒绝,仅显式授权工作区可读写,工作区外文件读不到)+ 网络默认拒绝(按会话网络模式注入互联网能力 SID 放行,放行还需宿主防火墙开)+ 擦密钥环境 + 容器专用临时目录;**运行时自检**(每进程实跑一条 native 探针、验证输出真能回传)决定走 AppContainer 还是降级——各版本以实测为准、不赌版本号(注册表开关无文档、各版本 console 行为有别);自检不过(旧版无 ConPTY / 开关无效 / 输出不回传)或容器创建失败时 fail-closed 降级到受限令牌沙箱(0.0.23 的剥权 + Job Object),绝不裸跑;最低支持 Win10 1809(ConPTY 底线);④ 顺带修了 0.0.39 的 R3(后台命令绕过沙箱)并随本版一并生效。真机(Win11)端到端验证:文件隔离(内可读 / 外被挡)、网络默认拒绝、native 命令在工作区 cwd 跑且输出回传,全部通过 | Claude Code |
| 0.0.39 | 命令沙箱加固 + AppContainer 隔离基座(以内部 / 安全改进为主)。① **安全修复**:堵住「后台运行命令完全绕过沙箱裸跑」的 fail-open 漏洞——此前 `background:true` 的命令直接以普通进程执行、不受沙箱约束,现在后台与前台一致受沙箱设置约束,且沙箱内进程可被 `kill_shell` 正常终止(沙箱路径补齐取消支持);② **AppContainer 文件 / 网络隔离基座(M8.2,暂未启用)**:实现并真机验证了 Windows AppContainer 命令沙箱——文件路径隔离(容器默认拒绝大多数路径、仅对工作区目录显式 ACL 授权后可读写,工作区外文件读不到)、网络默认拒绝 + 按会话网络模式放行(注入互联网能力 SID;放行还需宿主 Windows 防火墙开启)、命令经 powershell 包装 + 擦密钥环境 + 容器专用临时目录。受阻于该 Windows 构建的一个底层限制——**容器内 native 命令(git / npm / node 等)的控制台输出无法回传父进程**(只有 PowerShell cmdlet 输出回得来;经 ConPTY 伪控制台方案验证仍无解,根因是 AppContainer 断开「容器进程→其子进程」的 I/O 继承),故本版以「基座」形态门控保留全部实现与隔离测试,**默认仍用现有受限令牌沙箱**(行为不变);待换稳定版 Windows / 真实无控制台进程环境复测后再评估启用 | Claude Code |
| 0.0.38 | 工程健康与内核整理(以内部改进为主)。① 自我示范:为项目自身配置 `.mdga/diagnostics`(Agent 改完 Rust 代码后自动跑 `cargo check --workspace` 自检)与自定义斜杠命令 `/check`、`/release`,既 dogfood 自身可扩展机制、又强化「写完即验」;② 补 50 个集成测试(会话/消息持久化端到端、文件操作路径越界守卫、MCP JSON-RPC 解析),提升回归保护;③ Token 价格表加明确标注:为手工维护的快照、需随官方调价新增版本,非 DeepSeek 供应商成本显示「—」;④ 架构整理:把 Agent 内核的纯逻辑(身份/工具纪律/行为准则、工作区消息构建、上下文压缩阈值、验证命令探测、用量合并)从桌面端抽入独立的 `agent-core` 内核 crate 并补单测,为可独立测试 / 将来多端复用打基础(行为不变) | Claude Code |
| 0.0.37 | 内部重构(无功能变化):把 4400 行的前端单体 `App.tsx` 拆成 6 个文件(类型 / 工具函数 / 消息渲染组件 / 设置组件 / 弹窗组件 + 主组件),App.tsx 降到 2284 行。纯代码搬移、行为 / UI / 逻辑零变更;收益是前端从此可组件级维护、能并行开发、改动风险更低。为后续 App 主组件按 hooks / 视图进一步解耦打基础 | Claude Code |
| 0.0.36 | Agent「求真」能力加固(语言无关)。① 接地纪律:Agent 对某文件 / 模块 / 测试覆盖 / 依赖下结论前,必须真的读过 / 概览过 / 跑过对应命令,**禁止凭依赖清单或文件名臆断**(「依赖少 / 文件小」≠「空壳」);可度量的事实(测试数 / 构建状态 / 规模)一律用项目自身工具(cargo / npm / pytest / go test / maven… 按项目类型自动选)实测;做整库评估前先自核验,没证据的标注「未核实」,不把推断当事实输出;② 新增 `code_overview` 工具:一次性给出某文件 / 目录 / 整个项目的行数、公开符号、测试数量、构建文件,并在项目根给出按语言的聚合 + 建议的「求真」命令,跨 Rust / TS / JS / Python / Go / Java / C·C++ 等多语言——让 Agent「看清实质再下结论」成本极低,显著减少对代码库的误判 | Claude Code |
| 0.0.35 | 上下文呈现 + 全面体验复审落地。① 上下文指示器移入输入框:底栏显示上下文占用,点开看「窗口用量 / 上限」+(DeepSeek)余额;底栏胶囊统一弱化(更小字、半透明、细虚线)、布局对齐;② 推理过程可见:深度思考模型的思考链以可折叠「思考过程」卡片实时展示,不再只有一个 token 计数;③ 上下文窗口随供应商:可为每个供应商单独设上下文窗口,接近其真实窗口即自动压缩(修复非 DeepSeek 小窗口模型永不压缩→溢出);④ 助手消息「重新生成」;⑤ ask_vision:可对已上传图片追问细节(二次精读);⑥ 全局快捷键(Ctrl+N 新建 / Ctrl+K 命令面板 / Ctrl+, 设置)+ `/help` 能力说明;⑦ 对话正文搜索(不止标题);⑧ 无障碍:菜单方向键可达 + 弹窗焦点陷阱;⑨ 长会话分段渲染,滚动更顺;⑩ 权限设置可从最近被拦动作一键加规则 | Claude Code |
| 0.0.34 | Agent 核心加固 + 视觉收尾。核心：① 改完代码自动验证——发生写操作后自动跑构建 / 测试 / 诊断，失败时把报错回灌让 Agent 自纠（最多 2 轮），「写完即验」成为默认纪律；② 计划闭环——计划模式产出后多出「批准并执行计划」按钮，点击即自动建任务清单并严格按计划推进；③ 长任务更可靠——任务清单每轮重新提醒、持久化到工作区 `.mdga/tasks/current.json`，连续无进展或反复同样失败会自动暂停并提示你介入；④ 工具调用更稳——兼容更多模型把工具调用「泄漏」成正文的格式并恢复执行，供应商设置新增「测试工具调用」一键检测该模型工具调用是否可用；⑤ 新增 apply_patch（对一个文件一次性多处精确编辑、全有或全无、可回退）；⑥ 子代理可在权限与检查点保护下执行写操作（写模式仅前台、每步照常审批 / 留快照）。视觉：用户消息气泡更圆润、配色融入主题；消息悬浮的复制 / 重发 / 编辑重试改为纯图标（去框去底色去文字）；清理无用样式。内部：Agent 身份与新增的行为准则抽为可维护的核心提示模块 | Claude Code |
| 0.0.33 | 布局与输入区优化：① 输入区(composer)重做为一体化圆角面板——附件改为低调的「+」，权限 / 工作区 / 模型 / 计划 / 发送都收进输入框底部一行，不再是分立两侧、各自突兀的按钮；② 工作区入口移到输入框旁的胶囊：新建会话可直接选工作区或纯聊天，已有会话也能随时改绑工作区 / 切换纯聊天（改绑后下一轮即用新工作区，项目结构摘要重新生成），不必再去顶栏或首屏的独立按钮；③ 顶栏瘦身、首屏更聚焦输入；④ 滚动条精细化：更细、配色融入主题、贴在对话区右边界（直觉位置，也是未来停靠式第三栏的分界），侧边栏 / 设置 / 代码块滚动条统一变细 | Claude Code |
| 0.0.32 | 交互细节修复：① 根治「密钥要保存两次才生效」——此前在「设置 → 模型供应商」不重新输入密钥直接保存会把已存密钥清空，现在留空即保留原密钥（占位文案「已配置 ••••（如需更换请重新输入）」名副其实）；② 保存供应商后显示「已保存 ✓」明确反馈，不再让人误以为没生效；③ 设置 / 变更弹窗可点击窗内空白处或按 Esc 关闭，不必非得点左下角「关闭」；审批弹窗按 Esc 即拒绝、提问弹窗按 Esc 即取消；④ 改动供应商配置（密钥 / 端点 / 模型 / 格式）后，旧的「测试连接」结果会清除，不再残留误导；⑤ 按钮按下有轻微缩放反馈，点击手感更明确 | Claude Code |
| 0.0.31 | 操作安全与多供应商一致性：① 默认模式下删除文件 / 目录改为需逐次确认（写入仍自动），且无法回退的覆盖 / 删除（删目录、超大或二进制文件）会明确弹出「此操作不可回退」提示后再由你决定，杜绝默认模式下静默且不可恢复的数据丢失；② 多供应商一致性——配置非 DeepSeek 主供应商（智谱 / Kimi / 通义 / 自定义）时，余额查询提示「该供应商不提供余额查询」而非误报错误，成本金额显示「—」（不再按 DeepSeek 价表给出误导数字），token 数照常显示；③ 单轮 token 上限现在把视觉识图调用的开销也计入，标签正名为「单轮 token 上限」并说明每轮独立计算；④ 回退文件变更后，对话流里对应的变更卡片标记为「已回退」，与磁盘状态一致；⑤ 默认模式下联网搜索 / 抓取网页不再每次弹审批；⑥ 与内置命令同名的自定义斜杠命令会在菜单中标注「与内置命令冲突，已被忽略」 | Claude Code |
| 0.0.30 | 交互逻辑修复：① 多供应商主模型真正生效——实际对话用你在「设置 → 模型供应商」里配的模型（model_id），控制行改为显示当前模型、点击直达设置，不再被写死的 DeepSeek 模型名覆盖，非 DeepSeek 供应商（智谱 / Kimi / 通义 / 自定义）从此可正常对话；② 操作反馈不再丢失——图片门禁、导入 / 导出 / 回退 / MCP 等即时操作的成败改用右下角通知提示，此前在首屏等场景会无声消失；③ 删除会话加二次确认，避免误删；④ Agent 输出时可向上翻看历史不再被强制拽回底部，并提供「跳到最新」按钮；⑤ 发送失败时已附的图片不再丢失；⑥ 切换 / 新建会话不再残留上一会话的待发图片与计划模式开关；⑦ 修复含工具调用的回复末尾混入内部标记时，清理过程会误删工具 / 识图卡片的问题 | Claude Code |
| 0.0.29 | 交互体验补强：① 首屏修正——去掉过时的「API Key 只读环境变量」错误说明，未配置主模型时直接给「去配置模型供应商」引导，点击直达设置；② 截图直接用——支持把截图 Ctrl+V 粘贴、或把图片拖拽进输入框（已配视觉模型时），不必再走附件对话框；③ 消息级操作——悬停消息可一键复制整条，用户消息可重发 / 编辑后重试；④ 识图透明——识图时对话流内出现可折叠「视觉分析」卡片，看得到视觉模型识别了什么，并显示视觉 token 用量；视觉调用单独计入 Token 账本（CSV 导出含 vision 行），成本不再混入主模型；⑤ 供应商「测试连接」——配置后一键探测端点连通与鉴权，绿/红即时反馈，不必回主界面试发；⑥ 审批弹窗显示动作内容预览——执行高风险命令 / 写文件前，弹窗直接展示命令全文或写入内容，看清再决定是否允许 | Claude Code |
